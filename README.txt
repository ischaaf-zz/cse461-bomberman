setup:
install monogame with DirectX: PM> Install-Package MonoGame.Framework.WindowsDX

cloning:

clone the repo to a local folder
add repo to visual studio under source control
commit changes locally (within VS)
for pushing to gitlab you have to use the command line (git bash the folder with the project: git push)